---
# Launch instances, runs some tasks
# and then terminate them

- hosts: localhost
  connection: local
  gather_facts: False
  roles:
    - aws-network-delete
    - aws-security-delete
    - aws-cluser-delete
    - aws-database-delete
  vars:
    - region: us-west-2
